types:
  hello: string
  Person:
    name: string
    age: number
    address: any

  Address:
    main: string
    state: string | number
    zip: number

  Labs:
    students: Map<string, Person[]>
    amount: number

endpoints:
  ping:
    request:
      age: number
    response:
      message: string
