import {
  GetCourseBaseFolderIDRequest,
  GetCourseBaseFolderIDResponse,
} from "@/apis";

// getCourseBaseFolderID implements the getCourseBaseFolderID endpoint.
// This code has been automatically generated.
// You can move this function to other files within the /app/server/src/handlers directory,
// as long as the signature remains the same and the function is exported.
export const getCourseBaseFolderID = async (
  ctx: any,
  request: GetCourseBaseFolderIDRequest,
): Promise<GetCourseBaseFolderIDResponse> => {
  throw new Error("Not implemented");
};
